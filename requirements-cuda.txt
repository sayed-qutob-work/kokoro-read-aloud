# NVIDIA GPU install. Use INSTEAD of requirements.txt, not after it:
#
#   env\Scripts\python.exe -m pip install -r requirements-cuda.txt
#
# Identical to requirements.txt except that torch comes from PyTorch's own
# index as a CUDA build. Measured 25.03x RT for 348MB of VRAM on a GTX 1650,
# against 1.77x on the same laptop's CPU (AUDIT.md, 2026-08-11).
#
# cu126 wheels need a reasonably current NVIDIA driver. If torch installs but
# the GPU is not used, check it directly:
#
#   env\Scripts\python.exe -c "import torch; print(torch.cuda.is_available())"
-r requirements-base.txt
--extra-index-url https://download.pytorch.org/whl/cu126
torch==2.13.0+cu126
